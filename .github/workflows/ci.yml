name: Python CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.8'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: |
          pytest

  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Build the Docker image (optional)
        run: |
          docker build -t my-flask-app .

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      # Optional: Here you can add deployment steps to another platform.
      # For example, deploy to a cloud platform like AWS, Google Cloud, Azure, or another system.
      - name: Deploy to your chosen platform (AWS, Azure, etc.)
        run: |
          # Example for AWS Elastic Beanstalk or other deployment systems
          echo "Deploying to cloud platform"
          # Replace this with the appropriate deploy script for your platform
